# Legacy Code

This directory holds historical code and assets that are preserved for reference but are not part of the main `src/faketrace_app` runtime path.

Current contents:

- `Inference/`: old MARC-oriented inference package kept for history

If new product code is added, prefer placing it under `src/faketrace_app/`.
