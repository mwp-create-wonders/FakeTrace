# Audio Model Assets

Put an ATADD-compatible audio classifier checkpoint here as `best.pt`.

The default app config expects:

- checkpoint: `models/audio/best.pt`
- model family: `MIT/ast-finetuned-audioset-10-10-0.4593`
- checkpoint key: `model_state` or a raw PyTorch state dict
